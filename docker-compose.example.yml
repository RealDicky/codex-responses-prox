services:
  codex-proxy:
    build: .
    container_name: codex-responses-proxy
    restart: unless-stopped
    ports:
      - "31415:31415"
    volumes:
      - ./config/models.json:/app/config/models.json
    environment:
      - PORT=31415
